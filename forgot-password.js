const params=new URLSearchParams(location.search),form=document.querySelector('#forgot-form'),message=document.querySelector('#message');let companyId=params.get('tenant')||localStorage.getItem('pdl-company-id')||'';
if(companyId){localStorage.setItem('pdl-company-id',companyId);document.querySelector('#back-login').href=`/login.html?tenant=${encodeURIComponent(companyId)}`}
async function locateCompany(email){if(companyId)return companyId;const response=await fetch('/api/auth/company',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})}),data=await response.json();if(response.ok&&data.companyId){companyId=data.companyId;localStorage.setItem('pdl-company-id',companyId);document.querySelector('#back-login').href=`/login.html?tenant=${encodeURIComponent(companyId)}`}return companyId}
form.onsubmit=async event=>{event.preventDefault();message.textContent='Sending your reset link…';try{const email=document.querySelector('#email').value,tenant=await locateCompany(email);if(!tenant){form.innerHTML='<div class="auth-note success"><strong>Check your email.</strong><br>If that address belongs to an active account, a single-use reset link is on its way. It expires in 30 minutes.</div>';return}const response=await fetch('/api/auth/forgot',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json','X-PDL-Company':tenant},body:JSON.stringify({email})}),data=await response.json();if(!response.ok)throw Error(data.error);form.innerHTML='<div class="auth-note success"><strong>Check your email.</strong><br>If that address belongs to an active account, a single-use reset link is on its way. It expires in 30 minutes.</div>'}catch(error){message.textContent=error.message}};
